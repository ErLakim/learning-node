// Import the stringToUppercase function from lib-string.js
const { stringToUppercase } = require("./lib-string");

// Example usage
const originalString = "hello world";
const uppercaseString = stringToUppercase(originalString);

console.log(`Original string: ${originalString}`);
console.log(`Uppercase string: ${uppercaseString}`);
